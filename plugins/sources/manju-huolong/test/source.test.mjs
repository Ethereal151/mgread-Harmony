import assert from'node:assert/strict';import test from'node:test';import*as plugin from'../dist/index.mjs';test('Huolong source extracts stable cid and resolves play API',async()=>{const proxied=[];await plugin.activate({log:{info(){},warn(){}},resource:{proxy(v){proxied.push(v);return`http://127.0.0.1/r/${proxied.length}`;}},http:{async fetch(input){const url=String(input);if(url.includes('/detail?'))return Response.json({list:[{vod_name:'测试漫剧',vod_pic:'https://img.example/c.jpg',vod_play_url:'1$episode-token'}]});if(url.includes('/play?'))return Response.json({url:'https://media.example/1.m3u8'});return Response.json({list:[{vod_id:JSON.stringify({cid:'mzc123'}),vod_name:'测试漫剧',vod_pic:'https://img.example/c.jpg'}]});}}});const listing=await plugin.discover({target:'channel:manju',cursor:null,collectionId:null,pageSize:5}),item=listing.document.components[0].children[0].items[0].content;assert.equal(item.id,'video:mzc123');const chapters=await plugin.getChapters({id:item.id});assert.equal(chapters.items[0].id,'video:mzc123:0');const content=await plugin.getContent({id:item.id,chapterId:chapters.items[0].id});assert.equal(content.media.resourceType,'video');assert.equal(proxied.at(-1).url,'https://media.example/1.m3u8');});test('Huolong source reports the upstream upgrade sentinel as access blocked',async()=>{const raised=[];await plugin.activate({errors:{raise(value){raised.push(value);throw Object.assign(new Error(value.message),value);}},log:{info(){},warn(){}},resource:{proxy(){return'http://127.0.0.1/r';}},http:{async fetch(){return Response.json({list:[{vod_id:'upgrade_required',vod_name:'请更新到唐三最新版',vod_remarks:'找唐三'}]});}}});await assert.rejects(()=>plugin.discover({target:'channel:manju',cursor:null,collectionId:null,pageSize:5}),error=>error.code==='source_access_blocked');assert.deepEqual(raised,[{code:'source_access_blocked',message:'漫剧小龙上游服务当前要求升级，暂时无法读取内容。',annotation:'上游仅返回“请更新到唐三最新版”，未提供真实作品或分集目录。'}]);});
